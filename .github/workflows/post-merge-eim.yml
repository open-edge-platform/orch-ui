---
  # SPDX-FileCopyrightText: (C) 2025 Intel Corporation
  # SPDX-License-Identifier: Apache-2.0

name: Post-Merge Eim

on:
  push:
    branches:
      - main
      - release-*
    paths:
      - 'apps/eim/**'
      - 'library/**'
  workflow_dispatch:

jobs:
  post-merge-pipeline:
    uses: intel-tiber/orch-ci/.github/workflows/post-merge.yml@hotfix/version-check
    with:
      run_security_scans: true
      run_version_check: true
      run_dep_version_check: false
      run_build: true
      run_docker_build: true
      run_docker_push: true
      run_helm_build: true
      run_helm_push: true
      run_version_tag: true
      run_artifact: false
      prefix_tag_separator: "/"
      project_folder: apps/eim
      orch_ci_repo_ref: hotfix/version-check
    secrets: inherit
